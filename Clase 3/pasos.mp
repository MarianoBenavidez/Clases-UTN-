**si empezamos de 0:**
   *git init
   * git add .
   *git commit -m 'comentario x'
   git branch -M main

** debemos crear un repo en git hub:**
   * git remote add origin https://github.com/MarianoBenavidez/repo_de_prueba
   *git push -u origin main

** listo con esto tenemos el github subido